services:
  presence:
    build:
      context: .
      dockerfile: Dockerfile
    image: presence:local
    container_name: presence
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_PATH: /data/presence.db
      TZ: Europe/Warsaw
    volumes:
      - presence-data:/data

volumes:
  presence-data:
